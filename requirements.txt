# 필수 (ES 학습 + 평가 + 분석 전부 이것만으로 동작)
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7

# 선택: 행동 복제 초기화(rl/pretrain_bc.py)에 torch 필요. PPO 경로는 셋 다 필요
# gymnasium>=0.29
# stable-baselines3>=2.3
# torch>=2.0
